default:      &default
  adapter:    postgresql
  encoding:   unicode
  pool:       5
  username:   postgres
  password:
  host:       db
  port:       5432

development:
  <<:         *default
  database:   dispatch

test:
  <<:         *default
  database:   dispatch_test

production:
  <<:         *default
  database:   dispatch
